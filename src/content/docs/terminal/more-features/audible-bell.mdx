---
title: 终端响铃
description: >-
  在 Warp 中启用可听见的终端响铃，可由 ping 等 CLI 工具触发。
---

Warp 允许你启用可听见的终端响铃（默认禁用），可由多种 CLI 工具触发（例如 `ping -a`）。

* 在设置中，通过 **Settings** > **Features** > **Terminal** 启用终端响铃。
* 在 [Command Palette](/terminal/command-palette/) 中，搜索 "Enable/Disable Audible Terminal Bell"。
